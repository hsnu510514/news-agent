## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues. See `docs/agents/issue-tracker.md`.

### Triage labels

Using default triage labels mapped to the five canonical roles. See `docs/agents/triage-labels.md`.

### Domain docs

Uses single-context layout with one `CONTEXT.md` at root and `docs/adr/`. See `docs/agents/domain.md`.
